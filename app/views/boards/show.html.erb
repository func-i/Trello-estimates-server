<h1>
  <%= @board.name %>
</h1>

<% @lists.each do |list| %>
  <h2>
    <%= list.name %>
  </h2>
  <% if list.cards.count > 0 %>
    <table class="table table-striped table-bordered">
      <thead>
        <th class="card-name">Name</th>
        <th>Developers Estimation</th>
        <th>Managers Estimation</th>
        <th>Time Tracked</th>
        <th>Remaining Hours</th>
        <th>Performance</th>
      </thead>
      <tbody>
      <% list.cards.each do |card| %>
        <tr>
          <td class="card-name">
            <!-- ALL methods below are located within `boards_helper.rb` -->
            <%= linked_card_name(card) %>
          </td>
          <td>
            <%= dev_estimate = fetch_time_estimates(@board, card) %>
          </td>
          <td>
            <%= manager_estimate = fetch_time_estimates(@board, card, manager = true) %>
          </td>
          <td>
            <%= tracked_time = total_card_tracked_time(card) %>
          </td>
          <td>
            <%= calculate_time_remaining(dev_estimate, manager_estimate, tracked_time) %>
          </td>
          <td>
            <%= calculate_card_performance(dev_estimate, manager_estimate, tracked_time) %>
          </td>
        </tr>
      <% end %>
      </tbody>
    </table>
  <% else %>
    <div>
      No cards in this section
    </div>
  <% end %>
<% end %>
